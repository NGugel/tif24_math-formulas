<!--<encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">-->

<configuration>

    <property name="ROOT_LOG" value="logs/rootlog"/>

    <appender name="ROOT-LOGGER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${ROOT_LOG}.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/archived/rootlog_%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- each archived file, size max 10MB -->
            <maxFileSize>10MB</maxFileSize>
            <!-- total size of all archive files, if total size > 2GB, it will delete old archived file -->
            <totalSizeCap>2GB</totalSizeCap>
            <!-- 60 days to keep -->
            <maxHistory>60</maxHistory>
        </rollingPolicy>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
        <encoder>
            <pattern>%d %p %c{1.} [%t] %m%n</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="ROOT-LOGGER"/>
    </root>

</configuration>